language: node_js
node_js: 11
cache: yarn
after_success: yarn coverage
jobs:
  include:
    - stage: npm release
      script: yarn build
      deploy:
        provider: npm
        api_key: $NPM_API_KEY
        on: master